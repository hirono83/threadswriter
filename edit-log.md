# 수정 로그 (Edit Log)

> **이 파일은 초안 생성 AI가 관리합니다.**
> 원본 초안과 실제 게시된 최종본을 나란히 비교해서, 사용자가 매번 어떻게
> 고치는지의 패턴을 뽑아내는 곳입니다. "반응이 안 좋았다"보다 훨씬 강한
> 신호입니다 — 정확히 뭘 바꿨는지 보이니까요.

## 작동 방식

1. 매일 아침, 최근에 채워진 `drafts/*.md`의 **"최종 게시본"** 필드를 확인한다.
2. 최종 게시본이 원본과 다르면, 무엇이 바뀌었는지 비교해서 아래 "개별
   기록"에 한 항목을 추가한다 (날짜, 카테고리, 원본, 최종본, 바뀐 점,
   추정 교훈).
3. 같은 종류의 수정이 **3회 이상 반복되면** "누적 패턴"으로 승격하고,
   패턴이 충분히 확실해지면 `style-guide.md`의 규칙으로 편입시킨다.
4. 최종 게시본이 원본과 동일하거나("그대로 게시") 비어있으면("미게시")
   기록하지 않는다 — 정보가 없다.

## 개별 기록

*(아직 없음 — drafts 파일에 최종 게시본이 채워지면 여기 쌓입니다)*

<!-- 기록 형식 예시:
### 2026-09-02 · 투자 관점

**원본**: (초안 전문)
**최종본**: (게시된 전문)
**바뀐 점**: (예: 종결어미를 ~다체에서 ~죠체로 바꿈 / 마지막 문장 삭제 /
  숫자를 뺌)
**추정 교훈**: (예: 투자 글은 단정적인 다체보다 죠체를 선호하는 듯)
-->

---

## 누적 패턴 (3회 이상 반복된 것)

*(아직 없음)*

---

## style-guide.md로 승격된 규칙

*(아직 없음 — 패턴이 충분히 확실해지면 여기로 옮기고 style-guide.md에도
반영합니다)*
